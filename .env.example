# Copy to .env and fill in when deploying against GCP.
# Not required for local development - the pipeline defaults to
# local file ingestion + SQLite storage.

GCP_PROJECT_ID=
GCS_BUCKET_NAME=
GCS_PREFIX=
BQ_DATASET=medical_data
BQ_TABLE=canonical_records
GOOGLE_APPLICATION_CREDENTIALS=./service-account.json
